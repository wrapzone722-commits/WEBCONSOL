# 🚀 Деплой на Timeweb Cloud

Полное руководство по развертыванию AutoDetailHub на платформе Timeweb Cloud App Platform.

## Быстрый старт

### 1. Подготовка проекта

Проект уже готов к деплою на Timeweb Cloud через Docker:

- ✅ Dockerfile настроен
- ✅ Порт 8080 (стандарт для облачных платформ)
- ✅ Health check endpoint
- ✅ Production build
- ✅ Graceful shutdown

### 2. Регистрация в Timeweb Cloud

1. Перейдите на https://timeweb.cloud
2. Зарегистрируйтесь или войдите в аккаунт
3. Пополните баланс (минимум для тестирования)

### 3. Создание приложения

#### Вариант A: Через Docker Registry (рекомендуется)

**Шаг 1: Сборка и загрузка образа**

```bash
# 1. Соберите образ локально
docker build -t autodetailhub:latest .

# 2. Тегируйте для Docker Hub (или другого registry)
docker tag autodetailhub:latest YOUR_DOCKERHUB_USERNAME/autodetailhub:latest

# 3. Войдите в Docker Hub
docker login

# 4. Загрузите образ
docker push YOUR_DOCKERHUB_USERNAME/autodetailhub:latest
```

**Шаг 2: Создание App на Timeweb**

1. Войдите в [панель Timeweb Cloud](https://timeweb.cloud/my)
2. Перейдите в раздел **"App Platform"** или **"Приложения"**
3. Нажмите **"Создать приложение"**
4. Выберите **"Docker Registry"** или **"Контейнер"**
5. Укажите образ: `YOUR_DOCKERHUB_USERNAME/autodetailhub:latest`

**Шаг 3: Настройка приложения**

```yaml
# Основные настройки
Имя: autodetailhub
Регион: ru-1 (Москва) или nl-1 (Амстердам)
Порт контейнера: 8080
Health check path: /health

# Переменные окружения
NODE_ENV=production
PORT=8080
```

**Шаг 4: Настройка ресурсов**

Минимальная конфигурация для старта:

- **CPU**: 0.25-0.5 vCPU
- **RAM**: 512MB - 1GB
- **Диск**: 5GB

Рекомендуемая для продакшена:

- **CPU**: 1 vCPU
- **RAM**: 2GB
- **Диск**: 10GB

#### Вариант B: Через Git (если Timeweb поддерживает)

1. Загрузите код в GitHub/GitLab
2. Подключите репозиторий к Timeweb
3. Timeweb автоматически обнаружит Dockerfile
4. Настройте переменные окружения

### 4. Настройка домена

После деплоя Timeweb предоставит домен вида:

```
https://your-app.timeweb.cloud
```

Для своего домена:

1. В панели приложения найдите **"Домены"**
2. Добавьте свой домен
3. Настройте DNS записи у регистратора:
   ```
   CNAME your-domain.ru -> your-app.timeweb.cloud
   ```

### 5. SSL сертификат

Timeweb автоматически выпускает Let's Encrypt сертификат для:

- Стандартного домена (.timeweb.cloud)
- Вашего кастомного домена (после настройки DNS)

## Переменные окружения

В панели Timeweb добавьте переменные:

```bash
# Обязательные
NODE_ENV=production
PORT=8080

# Опциональные
PING_MESSAGE=pong

# База данных (если используете)
# DATABASE_URL=postgresql://user:password@host:5432/db

# Внешние сервисы
# STRIPE_SECRET_KEY=sk_live_...
# SUPABASE_URL=https://...
# SUPABASE_KEY=...
```

⚠️ **Важно**: Не коммитьте секретные ключи в код! Используйте переменные окружения.

## Мониторинг и логи

### Просмотр логов

В панели Timeweb:

1. Перейдите в приложение
2. Откройте вкладку **"Логи"**
3. Выберите период времени

### Метрики

Доступны метрики:

- CPU usage
- Memory usage
- Network I/O
- Request rate
- Response time

### Алерты

Настройте уведомления при:

- Высокой нагрузке CPU/RAM
- Недоступности приложения
- Ошибках 5xx

## Health Check

Приложение предоставляет endpoint для проверки здоровья:

```bash
GET /health
```

Ответ:

```json
{
  "status": "ok",
  "timestamp": "2024-01-17T12:00:00.000Z"
}
```

Настройте Health Check в Timeweb:

- **Path**: `/health`
- **Interval**: 30s
- **Timeout**: 3s
- **Unhealthy threshold**: 3

## Масштабирование

### Вертикальное масштабирование

Увеличьте ресурсы в настройках:

1. Перейдите в **"Настройки"** → **"Ресурсы"**
2. Выберите новый тариф
3. Подтвердите изменения

### Горизонтальное масштабирование

Если Timeweb поддерживает множественные инстансы:

1. Увеличьте количество реплик
2. Timeweb автоматически распределит нагрузку

## База данных

### Подключение PostgreSQL

Если используете базу данных Timeweb:

1. Создайте БД в панели
2. Получите `DATABASE_URL`
3. Добавьте переменную окружения в приложение
4. Приложение автоматически подключится при следующем деплое

Пример `DATABASE_URL`:

```
postgresql://user:password@postgres.timeweb.cloud:5432/autodetailhub
```

### Миграции

При первом деплое с БД:

```bash
# Выполните миграции через консоль Timeweb
# или добавьте в entrypoint script
node scripts/migrate.js
```

## Автоматический деплой

### Webhook для Docker Hub

1. В Docker Hub настройте webhook:
   - URL: предоставит Timeweb
   - При каждом push образа Timeweb будет автоматически деплоить

### CI/CD через GitHub Actions

```yaml
# .github/workflows/deploy-timeweb.yml
name: Deploy to Timeweb Cloud

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/autodetailhub:latest

      # Timeweb webhook будет автоматически триггериться
```

## Backup и восстановление

### Резервное копирование данных

Приложение предоставляет экспорт/импорт через UI:

1. Перейдите в **Настройки**
2. Раздел **"Резервная копия данных"**
3. Нажмите **"Выгрузить"** → сохраните JSON

### Автоматический backup (если используете БД)

```bash
# Создайте cron job для backup БД
0 2 * * * pg_dump $DATABASE_URL > /backups/db_$(date +\%Y\%m\%d).sql
```

## Troubleshooting

### Приложение не запускается

**Проверьте логи:**

1. Панель Timeweb → Логи
2. Ищите ошибки вида:
   - `Error: Cannot find module`
   - `Port already in use`
   - `Database connection failed`

**Решения:**

- Убедитесь что `PORT=8080` в переменных
- Проверьте что образ собран правильно
- Пересоздайте приложение с чистым образом

### Health check fails

```bash
# Проверьте endpoint локально
docker run -p 8080:8080 autodetailhub
curl http://localhost:8080/health
```

Если работает локально, но не на Timeweb:

- Проверьте настройки firewall
- Убедитесь что порт 8080 открыт
- Проверьте Health Check path в настройках

### Медленная работа

1. **Увеличьте ресурсы**: CPU/RAM в настройках
2. **Проверьте метрики**: CPU/Memory usage
3. **Оптимизируйте код**: добавьте кэширование

### Out of memory

```bash
# В панели Timeweb увеличьте RAM
Settings → Resources → Memory: 1GB → 2GB
```

Или оптимизируйте приложение:

- Добавьте `NODE_OPTIONS=--max-old-space-size=1536` (для 2GB RAM)

## Стоимость

Примерная стоимость на Timeweb Cloud (может меняться):

### Минимальная конфигурация

- **0.25 vCPU + 512MB RAM**: ~200-300₽/мес
- Подходит для: Тестирование, малая нагрузка

### Рекомендуемая конфигурация

- **1 vCPU + 2GB RAM**: ~800-1200₽/мес
- Подходит для: Production, средняя нагрузка

### PostgreSQL БД (опционально)

- **Shared план**: ~300-500₽/мес
- **Dedicated план**: от 1000₽/мес

💡 **Совет**: Начните с минимальной конфигурации и масштабируйте по мере роста.

## Чеклист перед деплоем

- [ ] Собран и протестирован Docker образ локально
- [ ] Образ загружен в Docker Registry
- [ ] Создано приложение в Timeweb Cloud
- [ ] Настроены переменные окружения
- [ ] Выбран регион (Москва или Амстердам)
- [ ] Настроен Health Check
- [ ] Подключен домен (опционально)
- [ ] Настроен SSL сертификат
- [ ] Настроены алерты и мониторинг
- [ ] Создана резервная копия данных

## Полезные ссылки

- [Timeweb Cloud](https://timeweb.cloud)
- [Документация Timeweb](https://timeweb.cloud/help/)
- [Docker Hub](https://hub.docker.com)
- [Dockerfile проекта](./Dockerfile)
- [Docker compose](./docker-compose.yml)

## Поддержка

Проблемы с деплоем:

1. Проверьте логи в панели Timeweb
2. Тестируйте образ локально: `docker-compose up`
3. Обратитесь в поддержку Timeweb: support@timeweb.cloud
4. Создайте issue на GitHub проекта

---

🚀 **Готово к деплою на Timeweb Cloud!**
